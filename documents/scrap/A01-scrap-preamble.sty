\usepackage{import}

\usepackage[
	dvipdfmx,
	setpagesize=false,
	bookmarksnumbered,
	colorlinks
]{hyperref}
\usepackage{pxjahyper}
\hypersetup{
	colorlinks=true,
	citecolor=orange,
	linkcolor=blue,
	% linkcolor=teal,
	% linkcolor=airforceblue,
	% linkcolor=bleudefrance,
	urlcolor=cyan,
}
\usepackage{url}

% その他設定
\renewcommand{\labelenumi}{(\arabic{enumi})}
\renewcommand{\labelenumii}{(\alph{enumii})}
\renewcommand{\labelenumiii}{(\roman{enumiii})}

% section 番号の後にピリオド追加
\makeatletter
\renewcommand{\@seccntformat}[1]{\csname the#1\endcsname.\quad}
\makeatother

\newcommand{\maru}[1]{(#1)}
\newcommand{\setlayout}[1]{
	\IfFileExists{../config/layout-#1.sty}
	{\usepackage{../config/layout-#1}}
	{
		\IfFileExists{../../config/layout-#1.sty}
		{\usepackage{../../config/layout-#1}}
		{The layout file does not exist.}
	}
}

\import{../config/}{global-commands.sty}
